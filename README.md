# Deutsch-Jozsa-algorithm-
The Deutsch-Jozsa algorithm is a classic example of "quantum supremacy" on a small scale. It was the first to show that a quantum computer could perform a specific task significantly faster than a classical one.  In short, it determines whether a "black box" function (an oracle) is constant or balanced.
